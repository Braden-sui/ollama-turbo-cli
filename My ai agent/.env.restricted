# Restricted Sandbox/Profile Preset
# Tight defaults for secure execution with minimal egress.

# Profile selector
SANDBOX_PROFILE=restricted

# Network policy: allowlist with no entries by default
SANDBOX_NET=allowlist
SANDBOX_NET_ALLOW=
SANDBOX_BLOCK_PRIVATE_IPS=1
SANDBOX_ALLOW_HTTP=0
SANDBOX_ALLOW_PROXIES=0

# Shell tool (read-only, narrow list)
SHELL_TOOL_ALLOW=1
SHELL_TOOL_ALLOWLIST=git status,git log,git diff,ls,dir,cat,type,python -V,python --version,pip list

# Fetch limits/timeouts (conservative)
SANDBOX_MAX_DOWNLOAD_MB=5           # 5 MB
SANDBOX_REQUEST_TIMEOUT=15          # seconds
SANDBOX_RATE_PER_HOST=12
SANDBOX_HTTP_CACHE_TTL_S=600
SANDBOX_HTTP_CACHE_MB=200
SANDBOX_HTTP_CACHE_READ=1

# Sandbox resource limits (conservative)
SANDBOX_MEM_MB=512
SANDBOX_PIDS=64
SANDBOX_DISK_MB=128
