repos:
  - repo: local
    hooks:
      - id: yaml-secret-scan
        name: YAML Secret Scan
        entry: python scripts/verify_yaml_secrets.py
        language: system
        files: \.(yml|yaml)$
