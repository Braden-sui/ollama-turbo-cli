# Global ignores
.DS_Store
Thumbs.db
*.log

# Python
.pytest_cache/
__pycache__/
*.pyc

# Node/JS
node_modules/
dist/
build/
coverage/
playwright-report/
test-results/

# Project caches and artifacts
.sandbox/
.windsurf/
**/target/

# Local env files in subprojects (already ignored where present)
# agent-desk-pro/.env.local is ignored by its own .gitignore

# Root environment files (do not commit secrets)
.env
.env.local
.env.*.local
.env.development
.env.production
.env.test
!.env.example
.sandbox/webcache/*.bin
.sandbox/webcache/*.json
.sandbox
*.jar

# Keep shared YAML in Git; ignore only local/secret variants
src/config/local.yaml
src/config/secrets.yaml
src/config/*.local.yaml
src/config/*override*.yaml

# Always ignore env files (except example)
.env
.env.*
!.env.example
