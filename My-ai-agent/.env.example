### Example .env (secrets only)
#
# Non‑secret runtime settings (timeouts, retries, tool knobs, web/sandbox policy,
# Mem0 local vectors, model defaults, etc.) are now configured via centralized
# cfg (see src/core/config.py) and should NOT live in .env.
#
# Keep only secrets or credentialed proxies here.

# ----- Frontend (safe) -----
VITE_API_BASE_URL=http://127.0.0.1:8000

# ----- Core authentication -----
# Ollama Turbo API key (required)
OLLAMA_API_KEY=

# ----- Mem0 Platform (optional) -----
# Provide only if using Mem0 cloud; omit for local OSS mode
MEM0_API_KEY=
MEM0_ORG_ID=
MEM0_PROJECT_ID=

# Optional identity tags (non‑secret but okay to keep here if you prefer)
MEM0_USER_ID=
MEM0_AGENT_ID=
MEM0_APP_ID=

# ----- Provider API keys (optional) -----
BRAVE_API_KEY=
TAVILY_API_KEY=
EXA_API_KEY=
GOOGLE_PSE_KEY=
GOOGLE_PSE_CX=
COHERE_API_KEY=
VOYAGE_API_KEY=

# ----- Proxies (optional; prefer cfg.web.* when possible) -----
# HTTP_PROXY=
# HTTPS_PROXY=
# ALL_PROXY=
# NO_PROXY=

# Notes:
# - All non-secrets are driven by cfg via ClientRuntimeConfig and WebConfig.
# - See CONFIG_REFERENCE.md for the mapping and default values.

# --- Suggested reliability/web defaults (optional) ---
# Exclude low-trust domains from citations (still used for discovery)
# WEB_EXCLUDE_CITATION_DOMAINS=wikipedia.org,reddit.com
# Cap grounding context to keep it sharp
# RAG_MAX_TOKENS=1200

# --- Suggested trust configuration (optional) ---
# Enable heuristic trust scoring and wildcard domain consideration. Wildcard does NOT auto-trust;
# acceptance is still gated by trust score. Defaults preserve current behavior (allowlist only).
# WEB_TRUST_MODE=heuristic
# WEB_TRUST_THRESHOLD=0.60
# WEB_ALLOWLIST_DOMAINS=*
# Keep recency discipline strict by default; only soft-accept undated when trust passes in wildcard/ML mode.
# WEB_DATELINE_SOFT_ACCEPT=0
# Fallbacks and telemetry
# WEB_ENABLE_ALLOWLIST_NEWS_FALLBACK=1
# WEB_DEBUG_METRICS=1
