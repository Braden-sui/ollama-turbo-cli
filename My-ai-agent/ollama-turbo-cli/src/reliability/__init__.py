"""Reliability Mode package: retrieval, grounding, guards, consensus, and evaluation.

This package is currently a no-op skeleton to allow incremental integration
without altering default behavior. Modules provide simple types and stubs that
return empty results unless explicitly enabled and wired in the client/CLI.
"""
