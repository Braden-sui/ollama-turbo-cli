# Permissive Sandbox/Profile Preset
# Applies generous defaults and broad access with critical safety checks.
# Values are only applied if missing when using apply_default_profile();
# override by setting your own env before running.

# Profile selector
SANDBOX_PROFILE=permissive

# Network policy
SANDBOX_NET=allow
SANDBOX_ALLOW_HTTP=1
SANDBOX_ALLOW_PROXIES=1
# Allow any host by default (subject to SSRF protections)
SANDBOX_NET_ALLOW=*
# Permit private/loopback IPs (note: SSRF and safety checks still apply)
SANDBOX_BLOCK_PRIVATE_IPS=0

# Shell tool
SHELL_TOOL_ALLOW=1
SHELL_TOOL_ALLOWLIST=*

# Fetch limits/timeouts
SANDBOX_MAX_DOWNLOAD_SIZE=104857600   # 100 MB (bytes)
SANDBOX_REQUEST_TIMEOUT=60            # seconds

# Resource limits for sandbox
SANDBOX_MEM_MB=8192
SANDBOX_PIDS=512
SANDBOX_DISK_MB=1024

# HTTP cache knobs (optional)
SANDBOX_HTTP_CACHE_TTL_S=600
SANDBOX_HTTP_CACHE_MB=200
SANDBOX_HTTP_CACHE_READ=1
SANDBOX_RATE_PER_HOST=60
